#!/bin/bash

set -e

name=$(basename "$PWD")
[[ -f .port ]] || flower pick-port

flower ps1 ln -f -s "$PWD/.port" ~/.pow/$name

echo
echo "  —————>  "$'\e[34;4m'http://${name}.dev/$'\e[0m'
echo
echo $'\e[1m'Note:$'\e[0m' "Pow 0.4 is needed for port file proxying support."
